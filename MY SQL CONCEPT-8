SOME OTHER QUERRIES RELATED TO MY SQL:-

1. WRITE A QUERRY TO GENERATE ALL THE CITY WHICH ARE HAVING EVEN ID ALSO ARRANGING CITY IN PROPER ORDER?

SOLUTION :- 

    ---------> SELECT DISTINCT(CITY), ID, NAME FROM RECORD WHERE MOD(ID,2)=0 ORDER BY CITY;
    
NOTE:- MOD:- MOD OR MODULUS IS THE COMMAND WHICH MEANS MULTIPLE.


2. WRITE  A QUERRY TO GENERATE ALL THE RECORD BETWEEN AGE GROUP 23-40?

SOLUTION :- 

    ---------> SELECT * FROM RECORD WHERE AGE BETWEEN 23 AND 40;
    
3. WRITE A QUEERY TO FINDOUT THOSE DEPARTMENT FORM TABLE RECORD WHERE SUM SALARY IS GREATER THAN OR EQUAL TO 30K?

SOLUTION:-

    --------> SELECT DEPARTMENT, SUM(SALARY) FROM RECORD GROUP BY DEPARTMENT HAVING SUM(SALARY)>=30000;
   
    
NOTE:- HAVING :- THIS COMMAND IS USED TO FINDOUT THE AGGERATION OF DATA.

4. WRITE A QUERRY TO PROVIDE THE INFORMATION ABOUT THOSE DEPARTEMENT WHERE AVERAGE SALARY IS GREATER THAN OR EQUAL TO 60K?

SOLUTION :-

    ----------> SELECT DEPARTMENT, AVG(SALARY) FROM RECORD GROUP BY DEPARTMENT HAVING AVG(SALARY)>=60000;
    
5. WRITE A QUERRY TO PROVIDE THE INFORMATION ABOUT THOSE CITY WHERE AVERAGE SALARY IS GREATER THAN OR EQUAL TO 45K?

SOLUTION:-

    ---------> SELECT CITY, AVG(SALARY) FROM RECORD GROUP BY CITY HAVING AVG(SALARY)>=60000;